__pycache__/
*.py[cod]
.DS_Store
.pytest_cache/
# 私有 Skill 不在此登记：每个私有 Skill 目录自带 `.gitignore`（内容 `*`）自封装排除，
# 公共版本库因此不含任何组织名或私有能力清单。契约见 rules/技能设计.md 6.2 节。

# Skill 分发包构建产物（本地生成，不入库）
skills/*.zip

# 实例面：文件一旦存在即人工真源，见 rules/控制面布局.md「data/ 写入规约」
# 必须带前导斜杠锚定顶层——无锚点会匹配任意层级同名目录，误伤已跟踪的
# templates/instance/（历史上曾叫 templates/data/，与此互为前车之鉴）
/data/
