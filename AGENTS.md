# 系统工程 · Agent 入口

## 上下文

- 工作区元规则与总路由：`../AGENTS.md`
- 本目录说明：`README.md`

## 本目录职责

- `rules/` 保存跨项目通用规则，每条规则只维护一个主题。
- `templates/` 保存可复用模板；模板不得复制会频繁变化的业务映射。
- `tools/` 保存规则校验、项目初始化和任务同步工具。
- `skills/` 保存工作区维护的 Skill 真源。

## 修改约束

- 先判断内容应归属根入口、系统规则、项目入口还是任务材料。
- 修改规则时同步修复引用、模板和校验工具；不要在项目入口复制正文。
- 保留兼容入口和受管理区块，除非任务明确要求迁移或删除。
- 涉及多个仓库时逐库检查状态，不提交、不推送，除非用户另有明确要求。
