# {{项目名}} · Agent 工作规则

开始工作前读取：

1. `_项目总览.md`
2. `_契约/当前状态.md`
3. `_知识库/index.md`

工作材料按真实事件放入 `YYYYMMDD_事件名/`。事件结束后：

1. 更新 `_项目总览.md` 的时间线和最新产物；
2. 把已经确认的变化更新到 `_契约/当前状态.md`；
3. 未确认内容保留在“待确认”；
4. 共享知识只引用，不擅自移动、覆盖或复制；
5. 对外发布、个人或医疗数据相关材料必须等待人工确认。

不要创建 node_X、阶段空目录、工作流状态机或重复材料副本。

