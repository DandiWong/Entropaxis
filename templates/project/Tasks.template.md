# Tasks · {{应用名}}

> 本地任务状态唯一真源。实施规格位于 `docs/YYYYMMDD_主题/04_Spec_<ID>.md`（独立 Spec 用 `<ID>_中文主题.md`），并与 Board 联动。本地进行中统一记 `active`（外部看板端仍可能显示 `in_progress`，属其自身状态口径）。

## Active

- [ ] `Task-1`: 初始化应用工程骨架与基础依赖

## Backlog

- 待规划功能列表
