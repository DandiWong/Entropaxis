# {{应用名}} · Agent 工作规则

本文件只保存 {{应用名}} 相对工作区通用规则的补充和覆盖；通用研发闭环、Git 规范、Spec/看板同步协议见下方链接，不在此复制。

## 工作区上下文

- 工作区总路由与通用规则：`{{ROOT_AGENTS_PATH}}`

## 按需入口

| 任务场景 | 读取真源 |
|---|---|
| 启动、使用、能力边界 | `README.md` |
| 产品定位、设计原则 | `PRODUCT.md` |
| 文档导航与方案入口 | `docs/README.md` |
| 任务规划与变更履历 | `docs/Tasks.md`、`docs/Changelog.md`（或 `docs/00_project/`） |
| 具体特性实现规格 (Spec) | 对应主题工作胶囊 `docs/YYYYMMDD_<ID>_中文主题/` |
| 配置与环境变量 | `.env.example`、`config/` |

不要预读整个 `docs/`、`runs/`、`test/` 或第三方依赖目录。

## 项目硬约束

- 核心架构与业务不变量（待补充：如数据保真、受控网络调用、禁止前端伪造进度等）。
- 目录与外部源码修改边界（待补充：如外部 vendor 源码只读、特定忽略目录等）。

## 本项目特有增补

- 任务/变更/基线真源位置：`docs/` 或 `docs/00_project/`。
- 测试命令：（待补充，如 `pytest` / `npm test` / `node --test` 等）。
