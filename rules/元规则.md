# 元规则

工作区一切任务的顶层判断框架。

1. **一个事实，一个归属**：规则写在唯一真源，其他位置只链接。
2. **分层覆盖**：根规则 → 系统规则 → 项目 `AGENTS.md` → 任务材料；越靠近任务的明确规则优先。
3. **薄入口、按需读取**：常驻入口只保留安全边界、路由和项目硬约束，不预载整库文档。
4. **代码与事实优先**：描述与实现不一致时，以当前代码和可验证状态为准，并回写对应真源。
5. **尊重边界**：保留受管理区块、第三方/上游仓库的局部契约和用户已有改动；不为统一措辞而重写。
