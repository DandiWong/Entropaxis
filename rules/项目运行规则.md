# 项目运行规则

> 20260818 精简：Dashboard 写入的操作细节（curl、source_ref、任务分流判据）已收敛至
> `internal-board` Skill（`.system/skills/internal-board/SKILL.md`）；本文件只保留项目组织与任务解析原则。

## 系统与项目

- `.system/` 只放通用规则、模板、初始化工具和系统专用 Skill。
- 每个项目放在系统工程之外，默认与系统工程同级。
- 不按需求、POC、Spec、交付等固定阶段预建目录。

## 工作区项目索引

> **唯一真源**：`.data/registry.md`（工作区实例数据，不分发）。
> Agent 需要项目 ID、主目录或口语关键词时，先读 `.data/registry.md`；
> 共享资料层目录（不属于 Dashboard 项目）以 [`.data/workspace-config.md`](../../.data/workspace-config.md) 为准。
> 若 `.data/registry.md` 不存在，先创建空白文件（保留表头与列定义），提示用户补填项目信息，再继续任务；不得以"未找到注册表"为由跳过项目解析。

## 时间线与交付物规范

- 有真实事件时创建 `YYYYMMDD_事件名/`。
- **文档落盘触发门禁**：并非每个问答、讨论或验证动作都要生成磁盘文件。日常技术咨询、命名建议、方案探索直接在对话中交互完成；运行测试默认只保留终端结果，不据此自动生成 HTML/Markdown 测试报告。仅当用户明确要求落档，或 Agent 询问“是否需要落入文档”并获得明确许可后，方可创建调研、方案、说明、报告/汇报等独立交付物。
- **交付物命名规范**（本节为交付物命名唯一真源，其他文件只引用不复制）：新建调研、需求、设计、方案、评审等独立交付物时（无论是否为中间产物），采用 `{{yyyymmdd}}_{{PascalCaseTopic}}.<ext>`，例如 `20260819_SystemDesign.md`、`20260819_RequirementsAssessment.md`。日期是有效事件日期；主旨使用英文 PascalCase 名词/动词短语，首字母大写，只含英文字母和数字，不使用空格、连字符或额外下划线。开发项目的 task spec 默认采用 `<ID>_<PascalCaseTopic>.md`；项目已有约定时沿用项目约定。
- **组织名称默认口径**：后续新建文件夹、文件或内容涉及组织名称时，如无明确说明，一律使用 [`.data/workspace-config.md`](../../.data/workspace-config.md) 中的默认名称口径（含禁用缩写与对外例外）；对外正式材料以收件方或官方品牌要求为准。
- **命名例外**：只有入口和工作流真源（`README.md`、`AGENTS.md`、`DECISIONS.md`、`PRODUCT.md`、`Tasks.md`、`ChangeLog.md`、`ReleaseNote.md`、`Benchmark.md`）、机器契约以及有正式收件方命名要求的专用模板保持固定名称。测试报告不属于例外，获准生成时使用日期命名，例如 `20260821_TestReport.html`。task spec 的例外见开发通用规则。新规则默认约束新建或本轮实际修改的文件；存量批量改名须有用户明确要求。
- **办公文件落盘后开文件夹**：本轮任务生成 `.docx`、`.xlsx`、`.csv`、`.pptx`、`.pdf` 或图片（`.png`/`.jpg`/`.gif`/`.webp`/`.svg`）等通用传递格式文件后，任务结束前调用系统文件管理器打开该文件所在目录，方便用户直接取用。平台命令：macOS → `open "<dir>"`；Windows → `explorer "<dir>"`；Linux → `xdg-open "<dir>"`。同一任务多个文件位于同一目录时只打开一次；位于不同目录时逐一打开。
- 材料少时直接平铺；材料明显变多后再建立 `原始材料/工作稿/定稿/`。
- 同一事件可以同时产生需求、评测、计划、培训或汇报材料。
- 不为了满足结构而创建空事件目录。

### 讨论沉淀文档结构

经充分讨论后落盘的调研、方案、问题排查、bug 修复记录等文档，按以下固定结构组织，保证日后回查可定位：

1. **主题与场景**：讨论对象是什么，锚定的具体场景/触发事件；
2. **Baseline**：讨论开始时公认的现状、口径或参照基线（含来源链接）；
3. **问题**：本次要解决的具体问题，与 baseline 的偏差；
4. **原因**：经查证的原因分析；未查明的直接写“原因未查明”，不写推测；
5. **解决方案**：已确认采用的方案及关键取舍；多方案对比时保留落选理由；
6. **遗留问题**：未决事项、待确认项、后续动作；已拍板的决策另按“决策落盘”规则同步 `_契约/当前状态.md`。

适用边界：

- 适用于：讨论后落盘的调研报告、方案设计、问题排查记录、bug 修复记录；
- 不适用于：会议纪要（专用 Skill 模板）、task spec（`开发通用规则.md`）、有固定收件方模板的对外交付物；
- 开发项目中：调研、排查、修复记录适用本结构；task spec 继续按 `开发通用规则.md` 的格式执行，两者不互斥——spec 记“怎么做”，排查记录记“为什么这么改”；
- 某要素确实不存在时写“无”并一句话说明，不整段删除，保留回查锚点。

### 通用项目分层结构（4 域 + 2 底册 + RawInput + Archive）

针对兼具“业务资料管理 + 会议调研论证 + 软件研发交付 + 运营成效沉淀”的综合项目，采用如下标准通用分层结构：

```text
<工作区根目录>/
├── 00_知识库/                         # ════════【工作区共享知识域】════════
│   │                                  # 跨项目复用的权威规范、行业标准、文献、手册、说明书
│   │                                  # 只读事实源；项目内通过 _知识库/index.md 引用，不复制
│   └── <领域名称>/                    # 按学科/业务域组织，如：权威资料与规范/、医学文献/
│
<项目根目录>/
├── CLAUDE.md                          # [Claude Code 入口] 导入 @AGENTS.md
├── AGENTS.md                          # [Agent 控制面] 本项目路由、硬约束与 RawInput 整理规则
├── README.md                          # [总纲] 项目定位、目标、里程碑与目录全景导航（只导航，不存正文）
├── RawInput/                          # [暂存投递箱] 随手丢入的原始文件（触发”整理”后自动归位并清空）
│   └── .gitkeep
│
├── 01_项目管理/                       # ════════【业务与管理域 · 输入与过程】════════
│   ├── DECISIONS.md                   # [决策持久化] 跨会话唯一事实（记录已拍板生效的决策、基线与口径）
│   ├── 01_项目资料/                   # 项目专属原件：临床方案、CSR/实验报告、参考数据集（只读事实源）
│   │                                  # 跨项目通用规范/文献 → 工作区 00_知识库/，不放此处
│   ├── 02_调研评估/                   # 行业调研、PoC体验评估、迭代评测、方案规划（按 YYYYMMDD_事件名/ 组织）
│   │                                  # 含评估过程会议纪要；判断标准：产出是”认知”而非正式决策
│   ├── 03_决策记录/                   # 正式决策节点：立项批准、方案选定、上线放行、里程碑评审决议
│   │                                  # 判断标准：有明确决策结论（可含会议纪要、邮件批复、审批截图）
│   └── 04_价值回收/                   # 立项文件、战略汇报PPT、高管演示看板、成效推介、业务ROI测算
│
├── 02_产品设计/                       # ════════【产品定义域 · 需求与体验】════════
│   ├── 01_需求清单/                   # 业务功能列表、优先级跟进表、用户故事 (User Stories)
│   ├── 02_原型Demo/                   # 交互原型、静态/动态 UI Mockup、体验演示包
│   └── 03_设计规范/                   # 详细产品需求文档 (PRD)、设计系统 (Design System) 与交互规则定义
│
├── 03_工程研发/                       # ════════【工程实现域 · 技术与代码】════════
│   └── <app-repo-name>/               # 独立 Git 代码仓库（内部遵循下方 docs/ 00~06 分类）
│       ├── PRODUCT.md                 # 产品边界定义
│       ├── Tasks.md                   # [任务与规划真源] 唯一任务状态与需求规划真源（融合 TODO/Backlog）
│       ├── src/ / server/             # 源代码
│       ├── test/ / tests/             # 测试套件
│       └── docs/                      # 绑定在代码库的工程技术文档 (00~06)
│           └── 00_project/
│               ├── ChangeLog.md       # [变更日志] 研发代码与 Prompt 演进明细 (Keep a Changelog)
│               └── Benchmark.md       # [基准评估] 迭代量化成效与前后指标对比 (原 marker.md)
├── 04_运营增长/                       # ════════【交付与运营域 · 推广与赋能】════════
│   ├── 01_上线发布/                   # 发版记录、发布包物料、上线 Checklist、已定稿交付物/待审样稿
│   │   └── ReleaseNote.md             # [发版说明/通告] 面向业务与用户的正式交付特性、价值、升级指引与已知限制
│   ├── 02_培训/                       # 用户操作手册、培训胶片、培训视频/材料
│   ├── 03_营销推广/                   # 宣发文章、宣传彩页、对外推介材料
│   ├── 04_数据/                       # 运营监控大屏、提效数据底册、使用量与成效统计
│   └── 05_用户反馈/                   # 试点/用户反馈跟进台账、用户痛点与改进建议
│
└── Archive/                           # [废弃归档域] 已被新方案替代的历史文件与过期备份（注明替代路径）
```

#### `RawInput/` 暂存投递箱与“整理”机制

1. **定位**：无负担的暂存投递区，用于临时放置微信/邮件附件、会议转写、截图、未命名草稿等。
2. **“整理”指令 SOP**：当用户在会话中发出“整理”、“整理 RawInput”或“归档收件箱”时，Agent 自动执行：
   - 扫描 `RawInput/` 下所有文件，解析文件名与正文内容；
   - 官方凭据/合同/底册/项目专属输入资料 $\rightarrow$ 规范重命名后移入 `01_项目管理/01_项目资料/` 对应子目录；权威规范/文献/手册 $\rightarrow$ 工作区 `00_知识库/`；
   - 调研/评估/方案 $\rightarrow$ `01_项目管理/02_调研评估/`；有明确决策结论的会议纪要/批复 $\rightarrow$ `01_项目管理/03_决策记录/`；提炼已拍板决策写入 `01_项目管理/DECISIONS.md`；
   - 立项文件/汇报PPT/成效推介 $\rightarrow$ `01_项目管理/04_价值回收/`；
   - 需求清单/原型/PRD设计 $\rightarrow$ 移入 `02_产品设计/` 对应子目录；
   - 代码/技术Spec/系统实现 $\rightarrow$ 移入 `03_工程研发/<app>/` 或其 `docs/`；
   - 交付样稿/培训材料/宣发/运营数据 $\rightarrow$ 移入 `04_运营增长/` 对应子目录；
   - 历史作废版本 $\rightarrow$ 移入 `Archive/` 并注明替代来源；
   - **清空 `RawInput/` 目录（保留 `.gitkeep`）**，向用户输出归档清单。

### `docs/` 分类（代码工程内部文档布局）

当项目进入 `03_工程研发/<app-repo-name>/` 阶段时，代码工程内部采用如下 `docs/` 分类，作为技术实现的唯一真源：

```text
03_工程研发/<app-repo-name>/docs/
├── README.md          # 文档导航、真源层级和当前入口
├── .board.json        # 跨系统绑定（启用时）
├── 00_project/         # 任务、变更记录、计划和决策
├── 01_research/        # 调研、分析、论证和受控研究材料
├── 02_product/         # 产品目标、需求、路线和介绍材料
├── 03_design/          # UI/UX、交互和领域方案
│   └── mockups/       # 界面与交互原型
├── 04_architecture/    # 架构、接口、数据模型、部署
│   └── specs/         # task ID 绑定的实施规格
├── 05_reports/         # 评审、审计、差距评估和测试报告
└── 06_archive/         # 已被替代且已标明当前真源的历史材料
```

- `PRODUCT.md`（存在时）定义最高层产品边界；`docs/README.md` 只导航，不复制正文。
- 权威顺序通常为：产品定义 → 产品文档 → 方案设计 → 专项架构 → task spec → 项目状态与证据。
- 同一内容只有一个真源，其他文档使用链接；不保留旧路径副本。
- 目录使用 `编号_名称` 固定阅读顺序；`docs/README.md` 与 `.board.json` 留根。
- `06_archive/` 只放已有替代真源的材料，并记录替代路径。
- 通用项目不创建根级 `docs/`；需要代码工程文档分类时，在 `03_工程研发/<app-repo-name>/` 运行 `init_app.py` 创建。
## 项目总览 (README.md)

`README.md` 承担项目全局总纲与导航：

- 项目身份、用途、目标和相关人员；
- 4 域目录分层结构全景导航；
- 按日期排列的关键里程碑与事件索引；
- 当前有效状态入口、最新产物与下一关键事件。

事件完成后追加或更新一行，不在总览中复制详细正文。

## 决策与基线底册 (01_项目管理/DECISIONS.md)

`01_项目管理/DECISIONS.md` 是项目跨会话唯一的当前事实真源（Single Source of Truth）：

- 只写已经确认或当前明确采用的内容（业务基线、核心架构选型、接口标准、排除范围）；
- 每项基线链接到时间线中的证据或产物；
- 新结论覆盖旧结论时更新当前状态，旧版本仍留在原事件目录或保留演进说明；
- 未确认内容放在"待确认/待决事项"，不冒充当前事实。

### 决策落盘（跨会话记忆）

`01_项目管理/DECISIONS.md` 同时是**跨会话、跨 agent 的持久记忆层**——没有落盘的决策等于没有记住，下一次会话或换一个 agent 就会丢失。因此：

- 每当在对话或事件中**确认**一个决策、结论、基线或口径（不是临时讨论、不是待确认项），就把它写入所属项目的 `01_项目管理/DECISIONS.md`，并链接到时间线中的证据或产物。
- 触发点：用户明确表态"就这么定 / 确认 / 采用 X"、评审形成结论、机制或口径敲定、选型拍板等。纯讨论、假设、候选方案不落盘（放"待确认"或留在事件记录里）。
- 与 Dashboard 的边界：Dashboard 记**任务/状态/截止/相关人**，`DECISIONS.md` 记**当前以什么为准**（事实/决策/基线/口径），两者不重复。
- 存量项目目录尚无 `DECISIONS.md` 时，**首次出现确认决策**才创建它（用模板），不为占位预建空文件。
- 覆盖旧决策时更新 `01_项目管理/DECISIONS.md`，旧版本仍留在原事件目录，保留可追溯的演进。

不使用 `node`、`handoff_to`、`fields_origin` 或统一状态机。只有法规、验收或正式审批确有要求时，才增加独立契约文件。

## Agent 工作闭环

1. 读根 `AGENTS.md` 常驻层，按任务路由表按需读取规则与项目导航文档。
2. 把用户自然语言解析为任务卡，并用项目文件、Dashboard、日历和素材查证。
3. 确认本次事件目录和目标。
4. 在事件目录中处理材料。
5. 更新 README.md 和 DECISIONS.md 中发生变化的部分；确认决策落盘。
6. 需要登记任务时按 `internal-board` Skill §6 同步 Dashboard，并重新读取验证。
7. 报告产物位置、确认项、未决事项、下一步和 Dashboard 同步结果。

自然语言任务解析（任务卡元素、查证顺序、追问边界与示例）见 [`工作区指令.md`](工作区指令.md)。

## 与 Dashboard 的边界

- 项目目录保存材料、产物、时间线、结论、基线和知识。
- Dashboard 保存执行任务、状态、截止日期、相关人及通知状态。
- `DECISIONS.md` 的"下一步"只记录高层方向或关键事件，不复制 Dashboard 的详细任务清单。
- Agent 看板读写统一经 `internal-board` Skill（`dash.py` + Bearer Token）；不手写 HTTP 请求，不走本地端口或公开 Tunnel。
- 新任务必须带稳定 `source_ref`（格式与幂等行为见 `internal-board` Skill §6.1）。
- 对话中的结论、知识或材料不自动转成任务；只有明确行动项才进入 Dashboard。
- 安排会议只有在用户明确要求时才写入日历。
