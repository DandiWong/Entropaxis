# Batch export design

- Each batch is limited to 1,000 records.
- The acceptance gate requires evidence that the configured limit stays within the tested operating envelope.
- No load-test evidence exists for batches above 100 records.
